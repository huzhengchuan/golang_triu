package main

import (
	"fmt"
)

type base struct{
	name string
	age int
}

type student struct{
	base
}

func main() {
	fmt.Println("Hello golang!")
	var stu *student = new(student)
	stu.name = "hzc"
	stu.age = 15
	fmt.Print(stu)
}
